
   <%- include('partials/header') %>
   <div class="flex flex-wrap w-full lg:w-1/2 lg:mx-auto">

   <% users.forEach(user => { %>
    <div class="w-full">
    
      <div class="px-6 py-4 w-full mx-4 my-4 md:max-w-sm md:mx-auto rounded bg-white">
        <div class="font-bold mb-2"> <%= user.fullname %> <span class="inline-block bg-gray-200 rounded-full px-3 py-1 text-sm font-semibold text-gray-700 mr-2">SS3A</span></div>
        <p class="mb-3"><span class="text-gray-600">Lives in 🏡</span>: <%= user.residence %> </p>
        <p class="mb-3"><span class="text-gray-600">Occupation 👷‍♀: <%= user.occupation%></span> </p>
        <p class="mb-3">
          <span class="text-gray-600">Date of birth 📅: <%= user.month %> <%= user.year %></span> 
        </p>
      </div>
      <div class="">
      <% comments.forEach(comment => { %>
       
        <p class="px-6 py-4 w-full border-t border-gray-200 md:max-w-sm md:mx-auto rounded bg-white">
          <span class="text-sm bg-gray-200 p-3 rounded-full"><%= user.occupation %></span>
          <span><%= comment.comment %></span>
        </p>
    
      <% }) %>
    </div>

      <form action="/comments" method="POST" class="w-full p-3 mx-4 my-4 md:max-w-sm md:mx-auto rounded bg-white">
        <!-- <h3 class="mb-4">Comments</h3>
        <div class="">
        <div class="flex mb-2 py-2">
        <span><img class="h-12 sm:h-16 w-12 sm:w-16 rounded-full shadow" src="#" alt="profile"></span><p class="pl-4">Be the first to comment!</p>
        </div>
        </div> -->
      
      
        
        <label class="block pt-2"><strong class="mb-1">Add a comment!</strong>
        <div class="mb-2">
        <textarea name ="comment" rows="4" class="w-full block border border-gray-400 shadow-inner rounded bg-white resize-y p-2"></textarea>
        <div class="flex">
        <button type="submit" class="flex px-4 py-2 text-center rounded-sm items-center justify-center border border-grey-darkest tracking-wide bg-red-500 text-white hover:bg-grey-darker mr-2">Submit</button>
        </div>
        </div>
        </label>
        </form>
      </div>
    
         
    <% }) %>
  </div>

      
   
  <script src="src/index.js"></script>
</body>
</html>